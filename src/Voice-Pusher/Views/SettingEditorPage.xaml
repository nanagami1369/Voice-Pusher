<UserControl x:Class="Voice_Pusher.Views.SettingEditorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodel="clr-namespace:Voice_Pusher.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:SettingEditorViewModel}"
             d:DesignHeight="200"
             d:DesignWidth="40"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <ScrollViewer d:Background="Pink">
        <StackPanel>
            <Expander Header="設定" IsExpanded="True">
                <Expander Header="出力設定" IsExpanded="True">
                    <StackPanel>
                        <Label Content="出力するフォルダ" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="27" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Row="0" Grid.Column="0" BorderThickness="0" IsReadOnly="True" Text="%HOME%\Desktop" />
                            <Button Grid.Row="0" Grid.Column="1" Width="27" Height="27" HorizontalAlignment="Right" Content="📂" />
                        </Grid>
                        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" BorderBrush="#393939" BorderThickness="1" />
                        <Label Content="出力時に使用する文字コード" />
                        <ComboBox ItemsSource="{Binding EncodingList, Mode=OneTime}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding EncodingName, Mode=OneTime}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Label Content="命名規則" />
                        <TextBlock>
                            <Run Text="テスト→" />
                            <Run Text="{}{Number}_{Name}_{Script}" />
                        </TextBlock>
                        <WrapPanel>
                            <Button CommandParameter="{}{Number}" Content="連番" />
                            <Button CommandParameter="{}{Name}" Content="名前" />
                            <Button CommandParameter="{}{Script}" Content="台本" />
                            <Button CommandParameter="{}{VoiceActorName}" Content="声優" />
                            <Button CommandParameter="{}{LibraryName}" Content="ライブラリ名" />
                            <Button CommandParameter="{}{yyyy}" Content="年" />
                            <Button CommandParameter="{}{MM}" Content="月" />
                            <Button CommandParameter="{}{dd}" Content="日" />
                            <Button CommandParameter="{}{HH}" Content="時" />
                            <Button CommandParameter="{}{mm}" Content="分" />
                            <Button CommandParameter="{}{ss}" Content="秒" />
                        </WrapPanel>
                    </StackPanel>
                </Expander>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>